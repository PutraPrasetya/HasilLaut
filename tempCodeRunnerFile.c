#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x;
    for (int x=0; x <=10; x++)
    {
        if(x%2 == 0)
        {
            printf("nilai counter dengan bilangan genap: %d", counter);
        }
    }
    printf("sudah di luar kondisi for.");
}